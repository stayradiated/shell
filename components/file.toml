name = "file"
from = "base"

version = "1:5.32-*"

devDependencies = [ "apteryx" ]

build = """
apteryx file='{{VERSION}}'
"""

exports = [
  '/etc/ld.so.cache',
  '/etc/magic',
  '/etc/magic.mime',
  '/usr/bin/file',
  '/usr/lib/file/',
  '/usr/lib/x86_64-linux-gnu/libmagic.so.*',
]

