name = 'base'
fromImage = 'phusion/baseimage:noble-1.0.2' # 24.04

baseExportDir = '/exports/'

# todo: upgrade this version to `noble-1.0.2`
# we are already using noble, I just forgot to update the version
version = 'noble-1.0.2'

build = """
echo {{VERSION}}

export LANG=en_NZ.UTF-8
locale-gen $LANG
"""

[checkUpdates]
  type = 'github-release'
  url = 'https://github.com/phusion/baseimage-docker'
