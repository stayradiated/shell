name = 'ncu'
from = 'base'

# https://www.npmjs.com/package/npm-check-updates
version = '10.2.2'

dependencies = [
  'node'
]

build = """
npm install -g 'npm-check-updates@{{VERSION}}'
"""

exports = [
  '/usr/local/lib/node/'
]
