name = "acpi"
from = "base"

version = "1.7-1.3build1"

devDependencies = ["apteryx"]

build = """
apteryx acpi='{{VERSION}}'
"""

exports = ['/usr/bin/acpi']

[checkUpdates]
type = 'apt'
name = 'acpi'
