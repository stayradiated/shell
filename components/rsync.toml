name = "rsync"
from = "base"

version = "3.2.3-*"

devDependencies = [ "apteryx" ]

build = """
apteryx rsync='{{VERSION}}'
"""

exports = [
  '/usr/bin/rsync',
]

[checkUpdates]
  type = 'apt'
  name = 'rsync'
